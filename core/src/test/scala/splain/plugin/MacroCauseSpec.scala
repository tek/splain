package splain.plugin

import splain.SpecBase

class MacroCauseSpec extends SpecBase.File {

  check("sanityCheck") {
    checkError()
  }

  check("direct") {
    checkError()
  }

  check("indirect") {
    checkError()
  }
}
